# Integration test extractions

Extracted markdown and embedded images from text extraction (OCR) integration tests are saved here so you can inspect the results in detail after a test run.

These files are gitignored and will never be committed.
